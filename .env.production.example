# =============================================================================
# RELAYDESK PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# Copy the relevant sections to your deployment platforms:
# - Render: For backend variables
# - Vercel: For frontend variables
# =============================================================================

# -----------------------------------------------------------------------------
# RENDER (Backend) - Add these in Render Dashboard
# -----------------------------------------------------------------------------

# Django Core
DJANGO_SETTINGS_MODULE=relaydesk.settings.prod
SECRET_KEY=<auto-generated-by-render-or-use-strong-random-key>

# Database (auto-linked from PostgreSQL service)
DATABASE_URL=<from-render-postgresql-service>

# Redis (auto-linked from Redis service)
REDIS_URL=<from-render-redis-service>

# Security & CORS
ALLOWED_HOSTS=your-backend-name.onrender.com
CORS_ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-frontend-git-main.vercel.app
CSRF_TRUSTED_ORIGINS=https://your-frontend.vercel.app,https://your-frontend-git-main.vercel.app

# Performance
PYTHONUNBUFFERED=1

# Optional: Email (if using email features)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-specific-password

# -----------------------------------------------------------------------------
# VERCEL (Frontend) - Add these in Vercel Dashboard → Environment Variables
# -----------------------------------------------------------------------------

# API Configuration
NEXT_PUBLIC_API_URL=https://your-backend-name.onrender.com
NEXT_PUBLIC_WS_URL=wss://your-backend-name.onrender.com

# App Configuration
NEXT_PUBLIC_APP_NAME=RelayDesk
NEXT_PUBLIC_ENV=production

# Optional: Analytics & Monitoring
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_ANALYTICS_ID=

# =============================================================================
# IMPORTANT NOTES
# =============================================================================
#
# 1. Replace ALL placeholder values:
#    - your-backend-name → actual Render service name
#    - your-frontend → actual Vercel project name
#
# 2. SECRET_KEY: Use Render's "Generate Value" feature or create with:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# 3. DATABASE_URL & REDIS_URL: These are auto-populated when you link services
#    in Render dashboard
#
# 4. CORS_ALLOWED_ORIGINS: Include ALL Vercel preview URLs if needed:
#    - Production: https://your-app.vercel.app
#    - Preview: https://your-app-git-branch.vercel.app
#
# 5. For testing: You can temporarily set CORS_ALLOWED_ORIGINS=* but NEVER
#    use this in production!
#
# =============================================================================
