services:
  - type: web
    name: relaydesk-backend
    env: python
    region: oregon
    rootDir: backend
    plan: free
    autoDeploy: true
    buildCommand: |
      pip install -r requirements.txt && \
      python manage.py collectstatic --noinput --clear
    startCommand: gunicorn relaydesk.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120
    healthCheckPath: /api/health/
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: relaydesk.settings.prod
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: ALLOWED_HOSTS
        value: relaydesk-backend-ydus.onrender.com,relaydesk-backend-kd7f.onrender.com,relaydesk-backend.onrender.com
      - key: CORS_ALLOWED_ORIGINS
        value: https://relaydesk-on95.vercel.app,https://relaydesk.vercel.app,https://relaydesk-frontend.vercel.app
      - key: CSRF_TRUSTED_ORIGINS
        value: https://relaydesk-on95.vercel.app,https://relaydesk.vercel.app,https://relaydesk-frontend.vercel.app
      - key: PYTHONUNBUFFERED
        value: "1"
